{
  "name": "@selection-qx/server",
  "private": true,
  "type": "module",
  "exports": {
    "./hc": {
      "import": {
        "default": "./dist/hc.js",
        "types": "./dist/hc.d.ts"
      }
    }
  },
  "scripts": {
    "dev": "bun -b dev.ts",
    "build": "tsc -p tsconfig.build.json",
    "db:generate": "bun -b drizzle-kit generate",
    "db:push": "bun -b drizzle-kit push"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.4",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "better-sqlite3": "^12.6.0",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.9.3"
  },
  "prettier": {
    "plugins": [
      "prettier-plugin-organize-imports"
    ]
  }
}
